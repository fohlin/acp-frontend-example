{
  "name": "example-stencil-app",
  "private": true,
  "version": "0.0.1",
  "description": "Stencil App Example",
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec",
    "test.watch": "stencil test --spec --watchAll",
    "generate": "stencil generate"
  },
  "devDependencies": {
    "@stencil/core": "^2.3.0",
    "@types/jest": "^26.0.15",
    "jest": "^26.6.3",
    "jest-cli": "^26.6.3"
  }
}
